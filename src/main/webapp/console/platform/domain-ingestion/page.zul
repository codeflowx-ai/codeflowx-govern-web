<?page title="Domain Ingestion" contentType="text/html;charset=UTF-8"?>
<zk xmlns:n="native">
    <div class="container-fluid p-4" apply="org.zkoss.bind.BindComposer"
         viewModel="@id('vm') @init('com.codeflowx.platform.viewmodel.domainingestion.DomainIngestionDashboardViewModel')">
        
        <!-- Header -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h2 class="mb-1">
                            <i class="fas fa-cloud-upload-alt me-2"></i>
                            Ingesta de Dominios
                        </h2>
                        <p class="text-muted">Gestión de dominios de negocio y fuentes de datos</p>
                    </div>
                    <div>
                        <button class="btn btn-primary" onClick="@command('showCreateDomainWizard')">
                            <i class="fas fa-plus me-1"></i>
                            Crear Dominio
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Métricas Globales -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card border-0 shadow-sm">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted mb-1 small">Dominios Activos</p>
                                <h3 class="mb-0" label="@load(vm.activeDomains)"/>
                            </div>
                            <div class="bg-primary bg-opacity-10 rounded-circle p-3">
                                <i class="fas fa-database text-primary fa-lg"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="col-md-3">
                <div class="card border-0 shadow-sm">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted mb-1 small">Total Documentos</p>
                                <h3 class="mb-0" label="@load(vm.totalDocuments)"/>
                            </div>
                            <div class="bg-success bg-opacity-10 rounded-circle p-3">
                                <i class="fas fa-file-alt text-success fa-lg"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="col-md-3">
                <div class="card border-0 shadow-sm">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted mb-1 small">Jobs Activos</p>
                                <h3 class="mb-0" label="@load(vm.activeJobs)"/>
                            </div>
                            <div class="bg-warning bg-opacity-10 rounded-circle p-3">
                                <i class="fas fa-tasks text-warning fa-lg"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="col-md-3">
                <div class="card border-0 shadow-sm">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted mb-1 small">Tasa de Éxito</p>
                                <h3 class="mb-0">
                                    <label value="@load(vm.successRate)"/>%
                                </h3>
                            </div>
                            <div class="bg-info bg-opacity-10 rounded-circle p-3">
                                <i class="fas fa-chart-line text-info fa-lg"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Filtros y búsqueda -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-3">
                        <combobox class="form-control" placeholder="Industria"
                                  value="@bind(vm.filterIndustry)" buttonVisible="false"
                                  onChange="@command('applyFilters')">
                            <comboitem label="Todas las industrias" value=""/>
                            <comboitem label="Financial Services" value="Financial Services"/>
                            <comboitem label="Healthcare" value="Healthcare"/>
                            <comboitem label="E-commerce" value="E-commerce"/>
                            <comboitem label="Legal" value="Legal"/>
                            <comboitem label="Manufacturing" value="Manufacturing"/>
                        </combobox>
                    </div>
                    
                    <div class="col-md-2">
                        <combobox class="form-control" placeholder="Estado"
                                  value="@bind(vm.filterStatus)" buttonVisible="false"
                                  onChange="@command('applyFilters')">
                            <comboitem label="Todos" value=""/>
                            <comboitem label="Activo" value="active"/>
                            <comboitem label="Inactivo" value="inactive"/>
                            <comboitem label="Error" value="error"/>
                            <comboitem label="Borrador" value="draft"/>
                        </combobox>
                    </div>
                    
                    <div class="col-md-5">
                        <div class="input-group">
                            <span class="input-group-text bg-white">
                                <i class="fas fa-search text-muted"></i>
                            </span>
                            <textbox class="form-control" placeholder="Buscar dominio..."
                                     value="@bind(vm.searchText)" instant="true"
                                     onChange="@command('searchDomains')"/>
                        </div>
                    </div>
                    
                    <div class="col-md-2">
                        <button class="btn btn-outline-secondary w-100" onClick="@command('refreshDashboard')">
                            <i class="fas fa-sync-alt me-1"></i>Actualizar
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Grid de Dominios -->
        <div class="row g-4 mb-4">
            <div class="col-md-6 col-lg-4" forEach="@load(vm.domainsList)">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <!-- Header del dominio -->
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <div class="d-flex align-items-center">
                                <div class="me-3" style="font-size: 2rem;" label="@load(each.dindomainicon)"/>
                                <div>
                                    <h5 class="mb-0" label="@load(each.dindomainname)"/>
                                    <small class="text-muted" label="@load(each.dinindustry)"/>
                                </div>
                            </div>
                            <span class="@load(vm.getStatusColor(each.dinstatus))"
                                  label="@load(vm.translateStatus(each.dinstatus))"/>
                        </div>
                        
                        <!-- Descripción -->
                        <p class="text-muted small mb-3" label="@load(vm.truncate(each.dindomaindescription, 100))"/>
                        
                        <!-- Progress bar -->
                        <div class="mb-3" visible="@load(each.dinprogress gt 0)">
                            <div class="d-flex justify-content-between align-items-center mb-1">
                                <small class="text-muted">Progreso</small>
                                <small class="fw-bold"><label value="@load(each.dinprogress)"/>%</small>
                            </div>
                            <div class="progress" style="height: 6px;">
                                <div class="progress-bar" 
                                     style="@load(concat('width: ', each.dinprogress, '%'))"></div>
                            </div>
                        </div>
                        
                        <!-- Estadísticas -->
                        <div class="row g-2 mb-3">
                            <div class="col-4">
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="fw-bold" label="@load(each.dintotaldocuments)"/>
                                    <small class="text-muted d-block">Documentos</small>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="fw-bold" label="@load(each.dintotalwebpages)"/>
                                    <small class="text-muted d-block">Web Pages</small>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="fw-bold" label="@load(each.dintotalapis)"/>
                                    <small class="text-muted d-block">APIs</small>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Badges -->
                        <div class="mb-3">
                            <span class="badge bg-primary me-1" visible="@load(each.dinragenabled)">
                                <i class="fas fa-search me-1"></i>RAG
                            </span>
                            <span class="badge bg-success me-1" visible="@load(each.dintrainingenabled)">
                                <i class="fas fa-graduation-cap me-1"></i>Training
                            </span>
                            <span class="badge bg-warning" visible="@load(each.dinautoingestion)">
                                <i class="fas fa-sync me-1"></i>Auto
                            </span>
                        </div>
                        
                        <!-- Última actualización -->
                        <div class="mb-3">
                            <small class="text-muted">
                                <i class="fas fa-clock me-1"></i>
                                Actualizado: <label value="@load(vm.formatDate(each.dinlastupdate))"/>
                            </small>
                        </div>
                        
                        <!-- Acciones -->
                        <div class="d-flex gap-2">
                            <button class="btn btn-sm btn-outline-primary flex-fill" 
                                    onClick="@command('viewDomainDetails', domain=each)">
                                <i class="fas fa-eye me-1"></i>Ver
                            </button>
                            <button class="btn btn-sm btn-outline-secondary" 
                                    onClick="@command('editDomain', domain=each)">
                                <i class="fas fa-edit"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-success" 
                                    onClick="@command('manageSources', domain=each)">
                                <i class="fas fa-cog"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-danger" 
                                    onClick="@command('deleteDomain', domain=each)">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Jobs Recientes -->
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white py-3">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="fas fa-history me-2"></i>
                        Jobs Recientes
                    </h5>
                    <a href="javascript:void(0)" onClick="@command('viewAllJobs')">Ver todos</a>
                </div>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Job</th>
                                <th>Dominio</th>
                                <th>Tipo</th>
                                <th class="text-center">Estado</th>
                                <th class="text-center">Progreso</th>
                                <th>Fecha</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr forEach="@load(vm.recentJobsList)">
                                <td>
                                    <strong label="@load(each.dinjobname)"/>
                                </td>
                                <td>
                                    <label value="@load(vm.getDomainName(each.domain))"/>
                                </td>
                                <td>
                                    <span class="@load(vm.getJobTypeColor(each.dinjobtype))"
                                          label="@load(vm.translateJobType(each.dinjobtype))"/>
                                </td>
                                <td class="text-center">
                                    <span class="@load(vm.getJobStatusColor(each.dinstatus))"
                                          label="@load(vm.translateJobStatus(each.dinstatus))"/>
                                </td>
                                <td class="text-center">
                                    <div class="d-flex align-items-center justify-content-center">
                                        <div class="progress" style="width: 60px; height: 6px;">
                                            <div class="progress-bar" 
                                                 style="@load(concat('width: ', each.dinprogress, '%'))"></div>
                                        </div>
                                        <small class="ms-2"><label value="@load(each.dinprogress)"/>%</small>
                                    </div>
                                </td>
                                <td>
                                    <small label="@load(vm.formatDate(each.dincreatedat))"/>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>
</zk>


